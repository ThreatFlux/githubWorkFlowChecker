name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-unit:
    name: Test
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.0'
          check-latest: true

      - name: Install dependencies
        run: go mod download

      - name: Run tests with coverage
        run: |
          go test -v -coverprofile=coverage.txt -covermode=atomic ./cmd/... ./pkg/... ./tools/...
          make test

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: ThreatFlux/githubWorkFlowChecker

      - name: Run linter
        run: make lint

  test-e2e:
    name: End-to-End Tests
    if: github.ref == 'refs/heads/main'
    runs-on: self-hosted
    needs: test-unit
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.0'
          check-latest: true

      - name: Install dependencies
        run: go mod download

      - name: Run e2e tests
        env:
          GITHUB_TOKEN: ${{ secrets.GIT_TOKEN }}
        run: make test-e2e

  security-scan:
    name: Security Scan
    needs: [test-unit, test-e2e]
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.0'
          check-latest: true

      - name: Run vulnerability checks
        run: make vuln-check

  build:
    name: Build
    needs: [security-scan]
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.0'
          check-latest: true

      - name: Build binary
        run: make build

      - name: Build Docker image
        run: make docker_build

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: ghactions-updater
          path: bin/ghactions-updater
